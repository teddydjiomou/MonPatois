<%= form_for(@anglai) do |f| %>
  <% if @anglai.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anglai.errors.count, "error") %> prohibited this anglai from being saved:</h2>

      <ul>
      <% @anglai.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mot %><br />
    <%= f.text_field :mot %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
