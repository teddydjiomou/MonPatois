<%= form_for(@francai) do |f| %>
  <% if @francai.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@francai.errors.count, "error") %> prohibited this francai from being saved:</h2>

      <ul>
      <% @francai.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mot %><br />
    <%= f.text_field :mot %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
